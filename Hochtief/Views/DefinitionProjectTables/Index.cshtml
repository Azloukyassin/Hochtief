@model IEnumerable<Hochtief.Models.DefinitionProjectTables>

@{
    ViewData["Title"] = "Index";
}

    <h1>Alle Einträge</h1>

<p>
    <a asp-action="Create" class="btn btn-outline-info">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.sptable)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.accessTable)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SqlTable)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.sptable)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.accessTable)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SqlTable)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new {  id=item.id  }) |
                @Html.ActionLink("Details", "Details", new {  id=item.id  }) |
                @Html.ActionLink("Delete", "Delete", new {  id=item.id })
            </td>
        </tr>
}
    </tbody>
</table>
