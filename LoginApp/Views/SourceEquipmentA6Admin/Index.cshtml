@model IEnumerable<LoginApp.A6SourceEquipmenttest>

@{
    ViewBag.Title = "List";
}

<h2>Alle Einträge </h2>

<p>
    @Html.ActionLink("Neue Eintrag hinzufügen", "Create", "SourceEquipmentA6Admin")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.sourceEq_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.En_Equipment)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.De_Equipment)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Code)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.codeCompany)
        </th>
      
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.sourceEq_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.En_Equipment)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.De_Equipment)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Code)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.codeCompany)
        </td>
       
        <td>
            @Html.ActionLink("Update", "Update", new { id = item.sourceEq_id}) |

            @Html.ActionLink("Delete", "Delete", new { id = item.sourceEq_id })
        </td>
    </tr>
}

</table>
